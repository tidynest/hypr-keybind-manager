# Test configuration for Hyprland Keybinding Manager
# This file contains intentional conflicts for testing

# Working bindings
bind = SUPER, M, exec, kitty
bind = SUPER, Q, killactive
bind = SUPER, F, togglefloating
bind = ALT, TAB, cyclenext

# Conflict 1: SUPER+K used twice
bind = SUPER, K, exec, firefox
bind = SUPER, K, exec, chrome

# Conflict 2: ALT+M used twice
bind = ALT, M, workspace, 1
bind = ALT, M, exec, spotify

# More working bindings
bind = SUPER, Return, exec, alacritty
bind = SUPER, D, exec, wofi --show drun
